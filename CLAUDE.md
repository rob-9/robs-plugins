# robs-plugins

Obsidian plugin monorepo. Each subfolder is a standalone plugin symlinked into `~/obsidian/.obsidian/plugins/`.

## Rules

- When adding a new plugin, update the root `README.md` plugin table as well.
